我需要构建电子笔记的知识图谱，从文本中提取这些实体与实体间的关系，遵循以下指南：

- 提取文本有关主要内容的实体
- 将简写的实体名补充为完整形式
- 所有实体用中文表示
- 分析文本内容不需要将举例的故事作为实体
- 判断给定的实体列表中这些实体的关系，source和target都应该在给定的实体中
- 每个关系应该反映的是笔记的某种主要内容，包含这组实体之间完整的上下文、逻辑联系和背景信息，避免提取孤立的、缺乏意义的关系
- 请选择这笔记中最为典型、能够反映整体的实体关系
- 一组实体间只能有一个关系。请严格判断，不是所有的实体都需要有联系，如果没有找到有效关系，relations请返回空列表
- 给出提取关系的所在句子，只要原文，不要添加思考
输出格式：
   以 JSON 格式输出结果，结构如下：
   ```json
   {
     "entities": ["实体1", "实体2"...],
     "relations": [
       {
         "source": "实体1",
         "target": "实体2",
         "relation": "关系描述",
         "context": "所在句子"
       },
       // 更多关系...
     ]
   }
   ```
请分析给定的文本，并直接输出json，无需额外的解释说明。